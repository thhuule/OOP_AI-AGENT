# Local Artifacts

This directory is for generated demo output and temporary local artifacts.

For example, `demo_multi_agent` writes its report to `artifacts/demo/report.txt`.
Do not commit generated content from this directory.
